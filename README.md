# hello-world-java

A minimal Java Hello-World project.

How to run
- Using IntelliJ IDEA: Open the project and run the Main class.
- Using command line:
  - Compile: `javac src/Main.java`
  - Run: `java -cp src Main`
